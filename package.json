{
  "name": "qplus-voice-assistant",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @qplus/web dev",
    "dev:agent": "pnpm --filter @qplus/agent dev",
    "dev": "pnpm run --parallel dev:web dev:agent",
    "build": "pnpm run --parallel build",
    "test": "pnpm run --parallel test",
    "lint": "pnpm --filter @qplus/web lint"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}